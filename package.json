{
  "name": "colors-cc",
  "version": "1.0.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@10.32.0",
  "scripts": {
    "dev": "pnpm build:image-codecs && wrangler dev",
    "build:image-codecs": "node --import tsx scripts/build-image-codecs.ts",
    "build:worker": "pnpm build:image-codecs && WRANGLER_LOG_PATH=dist/wrangler.log wrangler deploy --dry-run --outdir dist/worker",
    "build:vps": "pnpm build:image-codecs && node --import tsx scripts/build-vps.ts",
    "build:all": "pnpm build:worker && pnpm build:vps",
    "deploy": "pnpm build:image-codecs && wrangler deploy",
    "deploy:worker": "pnpm validate && wrangler deploy",
    "deploy:vps": "sh scripts/deploy-vps.sh",
    "typecheck": "pnpm build:image-codecs && tsc --noEmit",
    "test": "pnpm build:image-codecs && vitest run",
    "test:watch": "pnpm build:image-codecs && vitest",
    "verify:seo": "tsx scripts/verify-seo.ts",
    "validate": "pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "@jsquash/oxipng": "2.3.0",
    "hono": "^4.0.0",
    "libimagequant-wasm": "0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "@types/node": "^24.13.3",
    "esbuild": "0.27.3",
    "googleapis": "^128.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.72.0"
  }
}
